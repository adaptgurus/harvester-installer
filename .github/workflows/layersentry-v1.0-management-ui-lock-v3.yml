name: LayerSentry v1.0 Management UI Image Lock v3

on:
  push:
    branches:
      - layersentry-v1.0-dev
    paths:
      - '.github/workflows/layersentry-v1.0-management-ui-lock-v3.yml'
      - 'scripts/layersentry/fetch-qualified-management-ui-lock-v3.sh'
  workflow_dispatch:
  schedule:
    - cron: '41 */6 * * *'

permissions:
  contents: write
  packages: read

concurrency:
  group: layersentry-v1.0-management-ui-lock-v3
  cancel-in-progress: false

jobs:
  verify-and-stage-v3-image:
    runs-on: ubuntu-22.04
    timeout-minutes: 85

    steps:
      - name: Checkout LayerSentry installer source
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: layersentry-v1.0-dev
          fetch-depth: 0
          persist-credentials: true

      - name: Wait for authoritative v3 management UI lock
        shell: bash
        env:
          LAYERSENTRY_UI_LOCK_MAX_ATTEMPTS: '30'
          LAYERSENTRY_UI_LOCK_SLEEP_SECONDS: '120'
        run: |
          chmod +x scripts/layersentry/fetch-qualified-management-ui-lock-v3.sh
          scripts/layersentry/fetch-qualified-management-ui-lock-v3.sh \
            build/locks/layersentry-management-server-v1.8.2-v3.lock

      - name: Load immutable v3 image coordinates
        id: lock
        shell: bash
        run: |
          set -euo pipefail
          lock=build/locks/layersentry-management-server-v1.8.2-v3.lock

          while IFS='=' read -r key value; do
            case "$key" in
              source_commit|qualification_workflow_run|immutable_image|digest_reference)
                echo "${key}=${value}" >> "$GITHUB_OUTPUT"
                ;;
            esac
          done < "$lock"

      - name: Pull exact image by digest
        shell: bash
        env:
          DIGEST_REFERENCE: ${{ steps.lock.outputs.digest_reference }}
          IMMUTABLE_IMAGE: ${{ steps.lock.outputs.immutable_image }}
        run: |
          set -euo pipefail
          docker pull "$DIGEST_REFERENCE"
          docker tag "$DIGEST_REFERENCE" "$IMMUTABLE_IMAGE"

      - name: Verify complete LayerSentry v3 UI inside image
        shell: bash
        env:
          IMAGE: ${{ steps.lock.outputs.immutable_image }}
          EXPECTED_SHA: ${{ steps.lock.outputs.source_commit }}
          EXPECTED_RUN: ${{ steps.lock.outputs.qualification_workflow_run }}
        run: |
          set -euo pipefail
          rm -rf /tmp/layersentry-v3-management-ui
          container_id="$(docker create "$IMAGE")"
          trap 'docker rm -f "$container_id" >/dev/null 2>&1 || true' EXIT

          docker cp "$container_id:/usr/share/harvester/harvester" /tmp/layersentry-v3-management-ui
          test -s /tmp/layersentry-v3-management-ui/index.html
          test -s /tmp/layersentry-v3-management-ui/layersentry-ui-build.json
          grep -R -a -m 1 'LayerSentry' /tmp/layersentry-v3-management-ui
          grep -R -a -m 1 'Sign in to LayerSentry' /tmp/layersentry-v3-management-ui
          grep -R -a -m 1 'Use the unique bootstrap password generated for this LayerSentry cluster.' /tmp/layersentry-v3-management-ui
          grep -R -a -m 1 'layersentry-operations-dashboard' /tmp/layersentry-v3-management-ui
          grep -F '"qualificationContract": "v3"' /tmp/layersentry-v3-management-ui/layersentry-ui-build.json
          grep -F "\"sourceCommit\": \"${EXPECTED_SHA}\"" /tmp/layersentry-v3-management-ui/layersentry-ui-build.json
          grep -F "\"qualificationWorkflowRun\": \"${EXPECTED_RUN}\"" /tmp/layersentry-v3-management-ui/layersentry-ui-build.json

      - name: Generate authoritative installer lock and chart override
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.lock.outputs.source_commit }}
          QUALIFICATION_RUN: ${{ steps.lock.outputs.qualification_workflow_run }}
          IMMUTABLE_IMAGE: ${{ steps.lock.outputs.immutable_image }}
          DIGEST_REFERENCE: ${{ steps.lock.outputs.digest_reference }}
        run: |
          set -euo pipefail
          mkdir -p build/locks build/values build/image-lists docs/build-evidence

          repository="${IMMUTABLE_IMAGE%%:*}"
          tag="${IMMUTABLE_IMAGE##*:}"

          cat > build/locks/layersentry-management-server-v1.8.2-v3.lock.json <<EOF
          {
            "schemaVersion": "1.0",
            "product": "LayerSentry",
            "qualificationContract": "v3",
            "component": "management-server-ui",
            "compatibilityBase": "rancher/harvester:v1.8.2",
            "sourceRepository": "adaptgurus/harvester-ui-extension",
            "sourceCommit": "${SOURCE_SHA}",
            "qualificationWorkflowRun": "${QUALIFICATION_RUN}",
            "image": "${IMMUTABLE_IMAGE}",
            "digestReference": "${DIGEST_REFERENCE}",
            "loginIdentity": "LayerSentry",
            "dashboardMarker": "layersentry-operations-dashboard"
          }
          EOF

          cat > build/values/layersentry-management-server-image-v3.yaml <<EOF
          image:
            repository: ${repository}
            tag: ${tag}
            pullPolicy: IfNotPresent
          EOF

          printf '%s\n' "$DIGEST_REFERENCE" > \
            build/image-lists/layersentry-management-server-v1.8.2-v3.txt

          cat > docs/build-evidence/layersentry-management-ui-v3-lock.md <<EOF
          # LayerSentry management UI v3 lock

          - Qualification contract: v3
          - UI source commit: ${SOURCE_SHA}
          - Qualification workflow run: ${QUALIFICATION_RUN}
          - Immutable image: ${IMMUTABLE_IMAGE}
          - Digest reference: ${DIGEST_REFERENCE}
          - Compatibility base: rancher/harvester:v1.8.2

          The pinned image was pulled by digest and inspected for the LayerSentry
          pre-authentication login, bootstrap-password instructions, recursively
          branded shell, and advanced Operations Control Plane.

          Downstream ISO inputs:

          - build/locks/layersentry-management-server-v1.8.2-v3.lock
          - build/locks/layersentry-management-server-v1.8.2-v3.lock.json
          - build/values/layersentry-management-server-image-v3.yaml
          - build/image-lists/layersentry-management-server-v1.8.2-v3.txt
          EOF

      - name: Export exact image for offline composition
        shell: bash
        env:
          IMAGE: ${{ steps.lock.outputs.immutable_image }}
        run: |
          set -euo pipefail
          mkdir -p offline-image
          docker save "$IMAGE" | gzip -9 > \
            offline-image/layersentry-management-server-v1.8.2-v3-docker.tar.gz
          test -s offline-image/layersentry-management-server-v1.8.2-v3-docker.tar.gz
          sha256sum offline-image/layersentry-management-server-v1.8.2-v3-docker.tar.gz > \
            offline-image/SHA256SUMS

      - name: Commit authoritative v3 installer lock
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.lock.outputs.source_commit }}
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            build/locks/layersentry-management-server-v1.8.2-v3.lock \
            build/locks/layersentry-management-server-v1.8.2-v3.lock.json \
            build/values/layersentry-management-server-image-v3.yaml \
            build/image-lists/layersentry-management-server-v1.8.2-v3.txt \
            docs/build-evidence/layersentry-management-ui-v3-lock.md

          if git diff --cached --quiet; then
            echo 'Authoritative v3 installer lock is already current.'
            exit 0
          fi

          git commit -m "build(layersentry): lock qualified v3 management UI ${SOURCE_SHA}"
          git push origin HEAD:layersentry-v1.0-dev

      - name: Upload offline v3 management UI image
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: layersentry-management-server-v1.8.2-v3-offline-${{ steps.lock.outputs.source_commit }}
          path: offline-image/
          if-no-files-found: error
          retention-days: 90
          compression-level: 0
